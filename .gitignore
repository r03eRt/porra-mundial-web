node_modules/
dist/
.DS_Store

# Contiene los PINs reales de los jugadores en claro: no subir al repo público.
supabase/seed-player-pins.sql

# Backups de la base de datos (datos, PINs en claro, usuarios auth): mantener privados.
supabase/backup/

# Credenciales de Supabase Auth de los jugadores (email + contraseña en claro): privado.
supabase/player-auth-credentials.txt

# App nueva del dashboard (build y deps locales)
admin-next/node_modules/
admin-next/dist/
